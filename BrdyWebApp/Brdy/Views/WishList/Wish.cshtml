@model Brdy.Models.WishListBirds

@{
    ViewData["Title"] = "WishList";
}

<h4>Wish List</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddToWishList">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CommonName" class="control-label"></label>
                <input asp-for="CommonName" class="form-control" />
                <span asp-validation-for="CommonName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="AddToWishList" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="DisplayWishList">Go to Wish List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}